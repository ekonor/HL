/*import '@angular/core/testing';
import { Arena } from './arena';

describe( 'Arena', () =>
{
  it( 'должно создать экзепляр класса', () =>
  {
    expect( new Arena() ).toBeTruthy();
  });

  it( 'должно принять значения в конструктор', () =>
  {
    let arena = new Arena( {
      id: 222,
      name: 'Центр подготовки супергероев',
      address: 'СПб, ул. Бассейная, д. 37',
      email: 'info@mail.ru',
      webSite: 'www',
      capacity: 444,
      logo: 'dir',
      cityName: 'New York'
    } );

    expect( arena.id ).toEqual( 222 );
    expect( arena.name ).toEqual( 'Центр подготовки супергероев' );
    expect( arena.address ).toEqual( 'СПб, ул. Бассейная, д. 37' );
    expect( arena.email ).toEqual( 'info@mail.ru' );
    expect( arena.webSite ).toEqual( 'www' );
    expect( arena.capacity ).toEqual( 444 );
    expect( arena.logo ).toEqual( 'dir' );
    expect( arena.cityName ).toEqual( 'New York' );


  });
});*/
